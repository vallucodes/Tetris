#pragma once

const int M = 20;
const int N = 30;

const int tileSize = 16;
const int wallTile = 10;

const int ScreenWidth = N * tileSize + 4 * tileSize;
const int ScreenHeight = M * tileSize + 4 * tileSize;


